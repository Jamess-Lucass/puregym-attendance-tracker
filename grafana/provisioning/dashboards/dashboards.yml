apiVersion: 1

providers:
  - name: puregym
    orgId: 1
    folder: PureGym
    type: file
    disableDeletion: false
    updateIntervalSeconds: 30
    allowUiUpdates: true
    options:
      path: /etc/grafana/provisioning/dashboards/json
